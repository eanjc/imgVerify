package test;

import java.awt.image.BufferedImage;
import java.io.File;
import java.util.HashMap;
import java.util.Map;

import javax.imageio.ImageIO;

import util.characterDivide;
import util.chartUtil;

public class StatisticZeroLocation {
/*
	public static void main(String[] args) throws Exception{
		// TODO 自动生成的方法存根
		int data[]=new int[200];
		for(int i=0;i<200;i++)
		{
			data[i]=0;
		}
		for(int i=1;i<=500;i++)
		{
			String sourceFile="C:\\文件\\毕业设计\\test\\18MF-9\\MF"+i+".jpg";
			File sf=new File(sourceFile);
			BufferedImage s=ImageIO.read(sf);
			characterDivide demo=new characterDivide(s);
			demo.scanWideZero();
			for(int p:demo.getZero())
			{
				data[p]++;
			}
			
		}
		String desFile="C:\\文件\\毕业设计\\test\\zeroStatistic.jpg";
		File df=new File(desFile);
		chartUtil cdemo=new chartUtil(data);
		cdemo.saveAsFile(cdemo.drawLineChart(), desFile, 2000, 500);
		
		Map<Integer,Integer>index=new HashMap<Integer, Integer>() ;
		for(int i=0;i<data.length;i++)
		{
			System.out.println(i+":"+data[i]);
		}

	}
*/
}
